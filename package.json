{
  "name": "david-workspace",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "node backend/server.js",
    "render-build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
